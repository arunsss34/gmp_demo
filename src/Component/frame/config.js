// src/config.js

const config = {
    agGrid: {
        headerBackgroundColor: 'blue', // Red
        rowBackgroundColor: '#fff',       // White
        oddRowBackgroundColor: '#f8d7da', // Light red
        evenRowBackgroundColor: '#f8d7da',// Light red
        fontColor: '#000'                  // Black
    }
};

export default config;
